apiVersion: v1
kind: Service
metadata:
  name: coursefind-api
  labels:
    app: coursefind-api
spec:
  type: ClusterIP
  selector:
    app: coursefind-api
  ports:
    - name: http
      port: 80
      targetPort: http
